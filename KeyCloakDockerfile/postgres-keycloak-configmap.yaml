apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-configmap
data:
  # Option A: Store the full connection string (recommended for simplicity if your app supports it)
  DATABASE_URL: "postgresql://majid:Alm1ghty1@postgres-service:5432/CBSAccount"
  # Option B: Store components separately (if your app reconstructs the string)
  DB_HOST: "postgres-service"
  DB_USER: "majid"
  DB_NAME: "CBSAccount"
  DB_PORT: "5432"